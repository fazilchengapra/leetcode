-- Write your PostgreSQL query statement below
select project_id,average_years from (select p.project_id, round(avg(e.experience_years), 2) average_years from Employee e join Project p on e.employee_id = p.employee_id group by p.project_id)